# DIVI — Python dependencies
#
# The version floors below are conservative. Before releasing, pin the exact
# versions of the environment that produced the reported results:
#
#     pip freeze > requirements-frozen.txt
#
# and commit both files. See README.md, section "Environment".

# --- core numerics ---
numpy>=1.26
pandas>=2.0
scipy>=1.11

# --- model and optimization ---
torch>=2.2

# --- baselines, metrics, datasets ---
scikit-learn>=1.3

# --- configuration files ---
PyYAML>=6.0

# --- figures ---
matplotlib>=3.7

# --- runtime and memory instrumentation (experiment_utils.get_peak_memory_mb) ---
psutil>=5.9

# --- 20 Newsgroups sentence embeddings (all-MiniLM-L6-v2) ---
# Required only by experiments/table13_runtime_real.py --dataset 20ng
sentence-transformers>=2.2

# --- Sparse K-means (SPKM) ---
# SPKM is not a Python package. experiments/table07_spkm.py shells out to R
# and requires the `sparcl` package. See R/install_sparcl.R.
